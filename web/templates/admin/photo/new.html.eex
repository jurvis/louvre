<div class="ui basic segment text container">
  <h1 class="ui dividing header">
    <%= link @post.title, to: admin_post_photo_path(@conn, :index, @post.slug) %>
    &raquo; New Photo
  </h1>

  <%= render "_form.html",
    changeset: @changeset,
    action: admin_post_photo_path(@conn, :create, @post.slug) %>
</div>
